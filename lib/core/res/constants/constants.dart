import 'package:flutter/material.dart';

const double kScreenVerticalPadding = 25;
const double kScreenHorizontalPadding = 25;
const EdgeInsets kScreenPadding = EdgeInsets.symmetric(
    horizontal: kScreenHorizontalPadding, vertical: kScreenVerticalPadding);
